# Archived Jekyll migration

This directory preserves the one-time importer and its original migration report for
historical reference. They are not part of the current authoring or build workflow.

The importer reflects the repository layout used during the Jekyll-to-Astro migration.
Do not run it against the current blog without reviewing and updating its output paths.
The current `content/` tree is the authoritative source.
